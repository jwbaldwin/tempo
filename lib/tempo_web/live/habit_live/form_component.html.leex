<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "habit-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= label f, :iterations %>
  <%= number_input f, :iterations %>
  <%= error_tag f, :iterations %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
